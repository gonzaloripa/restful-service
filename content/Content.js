//Modelo de usuarios
/*var mongoose = require('mongoose');  
var UserSchema = new mongoose.Schema({  
  name: String,
  email: String,
  password: String
});
mongoose.model('User', UserSchema); //Se vincula el esquema (todos los usuarios con nombre, email y pass) con el modelo llamado User
//Se utiliza el modelo para acceder a los datos de la base
module.exports = mongoose.model('User'); //Se exporta para usarlo en otros archivos
*/